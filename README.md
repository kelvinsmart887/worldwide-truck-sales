# Worldwide Truck Sales
Full Node.js/Express + SQLite truck marketplace. WhatsApp: +2348126578356.

Run: install Node.js 20+, run `npm install`, set `ADMIN_KEY` to a strong secret, then `npm start`.
Public site: `/`
Admin: `/admin.html`

For a real shareable domain, deploy this Node app to a hosting provider and attach a domain. I cannot register a domain or create an external hosting account from this chat without your account/authorization. For production, use PostgreSQL/object storage rather than local SQLite/uploads if the site will have many listings.
